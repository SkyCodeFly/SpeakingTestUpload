window.APP_CONFIG = {
  // For local development, leave this empty and the app will call the same origin.
  // For GitHub Pages, set this to your deployed backend, for example:
  // API_BASE_URL: "https://your-speaking-test-api.onrender.com"
  API_BASE_URL: "",
};
